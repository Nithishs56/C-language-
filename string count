#include<stdio.h>
#include<string.h>
int main()
{
  char str[]="this is a sample sentence";
  int count=0,i;
  for(i=0;str[i]!='\0';i++)
  {
    if(str[i]!=' ')
      count++;
  }
  printf("%d",count);
  return 0;
}
