#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello, World!";
    char sub[] = "World";

    if (strstr(str, sub) != NULL) {
        printf("Substring found!");
    } else {
        printf("Substring not found!");
    }

    return 0;
}
