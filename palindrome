#include<stdio.h>
#include<conio.h>
void main()
{
int num=1211111,r,on,rm;
on=num;
while(num!=0)
{
rm=num%10;
r=r*10+rm;
num/=10;
}
if(on==r)
{
printf("%d is a palindrome",on);
}
else
{
printf("%d is not a palindrome",on);
}
getch();
